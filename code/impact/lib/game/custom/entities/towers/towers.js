ig.module(
	'game.custom.entities.towers'
)
.requires(
	'impact.entity'
)
.defines(function(){

        EntityTower = ig.Entity.extend({

            init: function() {

            }
        })


});